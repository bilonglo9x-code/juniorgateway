[project]
name = "junior-proxy"
version = "0.1.0"
description = "OpenAI-compatible proxy for junior.so chat API"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.0",
    "httpx>=0.27.0",
    "uvicorn[standard]>=0.30.0",
]
